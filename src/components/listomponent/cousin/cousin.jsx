
// eslint-disable-next-line react/prop-types
const Cousin = ({name}) => {
    return (
        <div>
            <h2>Cousin</h2>
            <p>{name}</p>
        </div>
    );
};

export default Cousin;